# base_ui_flutter example

A minimal runnable application that demonstrates the `base_ui_flutter` package
by rendering its default `DesktopTokens.winForm` preset.

## Run

```sh
flutter run -d windows
```
